[package]
name = "ils_rust_fidelity"
version = "0.1.0"
edition = "2021"

[dependencies]
gpp_fidelity_core = { path = "../gpp_fidelity_core" }

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
