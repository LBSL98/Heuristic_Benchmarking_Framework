# M4 solver smoke metadata

This directory records metadata-only evidence for bounded solver-interface smoke checks over real-graph benchmark inputs.

These records may summarize local-only solver interface checks, command metadata, timeout status, output hashes and basic partition-file parseability. They do not authorize pilot/campaign execution, multi-dataset command plans, solver quality or runtime comparison claims, CART training, result-map updates, monograph claims, or raw/extracted/derived/input/output artifact commits.

## Current records

- `tigerline_roads_2025_06037_los_angeles_county_solver_smoke.yaml`: records bounded solver-interface smoke metadata for the local-only TigerLine Los Angeles County roads benchmark input.
