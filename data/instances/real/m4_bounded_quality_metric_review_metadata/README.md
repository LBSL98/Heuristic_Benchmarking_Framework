# M4 bounded quality-metric review metadata

This directory records reduced review metadata after bounded single-dataset quality-metric extraction gates.

These records may inspect already-versioned reduced metrics and document the next recommended gate. They do not execute solvers, recompute quality metrics, authorize a full campaign, create multi-dataset command plans, run synthetic expansion, run additional real graphs, run meta-heuristics, train CART, update result maps, write monograph claims, or commit raw/extracted/derived/input/output artifacts.

## Current records

- `tigerline_roads_2025_06037_los_angeles_county_bounded_quality_metric_review_1223.yaml`: records the bounded quality-metric review decision after the 1219 reduced quality-metric metadata.
