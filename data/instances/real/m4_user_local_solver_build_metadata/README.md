# M4 user-local solver build metadata

This directory records metadata-only user-local solver build or install attempts for real-graph confirmatory candidates.

These records may summarize user-local prefixes, source URLs, commits when available, build/install commands, binary paths, help/version probes, and checksums. They do not authorize TigerLine solver execution, pilot execution or re-execution, graph partition generation, campaign execution, multi-dataset command plans, meta-heuristic execution, performance or quality comparison claims, CART training, result-map updates, monograph claims, or raw/extracted/derived/input/output artifact commits.

## Current records

- `tigerline_roads_2025_06037_los_angeles_county_user_local_solver_build.yaml`: records the srv-noctua user-local METIS/KaHIP build or install attempt after apt/sudo remediation was blocked.
