# M4 bounded real-graph command-plan metadata

This directory records reduced command-plan metadata for bounded single-dataset real-graph execution gates.

These records may specify the execution scope, solver binaries, command templates, output directories, timeout policy, repetition policy and metadata guards. They do not execute solvers, authorize a full campaign, create multi-dataset command plans, run synthetic expansion, run additional real graphs, run meta-heuristics, train CART, update result maps, write monograph claims, or commit raw/extracted/derived/input/output artifacts.

## Current records

- `tigerline_roads_2025_06037_los_angeles_county_command_plan_1207.yaml`: bounded solver-only command plan for the TigerLine Los Angeles County roads 2025 instance after the successful limited pilot and post-pilot review.
