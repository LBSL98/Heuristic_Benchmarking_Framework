# 08_Results_to_Text_Map.md

## Purpose

This file maps empirical results to the exact claims they are allowed to support in the monograph. Its purpose is to prevent overclaim and to make every conclusion auditable.

## Mapping template

| Claim ID | Monograph claim | Supported by experiment(s) | Supported by figure/table | Support strength | Allowed wording | Forbidden wording | Notes |
|---|---|---|---|---|---|---|---|
| C-XXX |  |  |  | Strong / Partial / Weak |  |  |  |

## Rules

1. If a claim has no mapped support, it should not survive final revision.
2. “Strong support” means the evidence directly matches the claim scope.
3. “Partial support” requires narrower wording in the monograph.
4. “Forbidden wording” is mandatory whenever results are easy to overstate.

| Claim ID | Monograph claim | Supported by experiment(s) | Supported by figure/table | Support strength | Allowed wording | Forbidden wording | Notes |
|---|---|---|---|---|---|---|---|
| C-REPO-001 | The final repository state used in the monograph was stabilized through a controlled integration process, with code and documentation merged separately before final release integration. | EXP-REPO-001 | None | Strong | “the final repository state was stabilized through a controlled integration sequence” | “the repository is fully reproducible in every respect” / “all experimental claims are validated by this alone” | Governance/reproducibility support only; not performance evidence. |
| C-CALIB-001 | The benchmark release froze one global hyperparameter profile per stochastic participant after a bounded calibration stage. | EXP-CALIB-001 | None | Strong | “the benchmark release froze the stochastic profiles `grasp_b`, `ils_b`, `sa_e_maxsteps_100000`, and `ts_c` after bounded calibration” | “these are universally optimal hyperparameters” / “the calibration proves global superiority of any algorithm” | Supports calibration/methodology prose only. |
| C-PILOT-001 | The pilot executed the fully frozen benchmark protocol end-to-end and validated the artifact chain and schema-conformant outputs. | EXP-BENCH-PILOT-001 | None | Strong | “the pilot validated executability, artifact generation, and schema-conformant outputs under the frozen protocol” | “the pilot already establishes the comparative conclusions of the main benchmark” | Operational validation only. |
| C-PILOT-002 | The pilot review gate closed with full expected coverage and no schema errors. | EXP-BENCH-PILOT-001 | None | Strong | “the pilot review gate closed with 112 expected artifacts, 112 actual artifacts, and zero schema errors” | “the pilot proves algorithmic robustness in every scenario” | Supports audit/protocol prose only. |
| C-PILOT-003 | After successful pilot review, the benchmark tooling became admissible under the frozen protocol, but the main campaign still required any later gates such as the R1/R2/R3 panel-materialization gate. | EXP-BENCH-PILOT-001 | None | Strong for pilot-tooling transition; not sufficient for R1/R2/R3 claims | “after successful pilot review, the benchmark tooling became admissible under the same frozen protocol, subject to later gates such as the R1/R2/R3 panel-materialization gate” | “the main campaign no longer needs methodological caution” / “future protocol changes are impossible” / “pilot success alone proves three-regime coverage” | Governance/transition claim only. After `D-019`, main execution also required `EXP-PANEL-REGIME-GATE-001`. |

| C-PANEL-REGIME-GATE-001 | The main benchmark used an executable R1/R2/R3 instance panel before generating the official raw campaign. | EXP-PANEL-REGIME-GATE-001 | `364_plan_gate_audit.json`; `373_preflight_summary.json`; `375e_post_campaign_review_gate_final.json` | Strong for execution-governance and panel-coverage claims | “the official main benchmark was gated on and executed over a validated 12-instance panel with `R1=8`, `R2=2`, and `R3=2`” | “the panel size is sufficient for strong per-regime statistical inference” / “synthetic-only results establish conclusions for all regimes” | Supports panel eligibility and coverage, not performance conclusions by itself. |
| C-BENCH-MAIN-RAW-001 | The official R1/R2/R3 main campaign produced the expected reviewed raw artifact set under the frozen protocol. | EXP-BENCH-MAIN-001 | `375e_post_campaign_review_gate_final.json` | Strong for raw execution/artifact-readiness claims; not yet a final comparative-result claim | “the reviewed raw campaign produced 264 expected artifacts, all parseable and feasible, with `elapsed_ms` and `checkpoints[].time_ms` present” | “the final benchmark conclusions are already established” / “the selector dataset is already ready” / “collapsed statistical analysis is complete” | Collapse, statistical synthesis, and selector-ready construction remain pending. |
| C-BENCH-MAIN-OVERSHOOT-001 | Timeout overshoot occurred in timeout-status metaheuristic runs, but the audited clipped-time sensitivity check did not change winner identities under the gate comparison. | EXP-BENCH-MAIN-001 | `376_timeout_overshoot_and_tiebreak_audit.json` | Partial: supports caveat and non-blocking gate decision, not a claim that hard time limits were exact | “timeout overshoot was observed and is reported as a validity caveat; in the audited clipped-time sensitivity check, winner identities did not change” | “all algorithms stopped exactly at 5 seconds” / “overshoot is irrelevant” / “overshoot proves the benchmark is invalid” | Must be preserved in analysis prose and limitations. |
| C-BENCH-MAIN-COLLAPSE-001 | The reviewed R1/R2/R3 fixed-budget campaign produced a validated collapsed benchmark table at `T*=5000 ms`. | EXP-BENCH-MAIN-001 | `393_main_r1r2r3_fixed_budget_summary.json`; `394_validate_collapse_outputs.txt`; `395_independent_collapse_audit_summary.json` | Strong for fixed-budget collapsed-table existence and structural validity | “the reviewed campaign produced 72 collapsed fixed-budget rows and 12 winner rows over the common-feasible R1/R2/R3 panel” | “the final selector dataset is complete” / “all comparative and anytime analyses are complete” | Supports the fixed-budget collapse only. Budget-aware, selector, and expanded-panel analyses remain separate. |
| C-BENCH-MAIN-DOMINANCE-DIAGNOSTIC-001 | In the current 12-instance R1/R2/R3 panel at `T*=5000 ms`, the fixed-budget winner table is dominated by multilevel solvers. | EXP-BENCH-MAIN-001 | `393_main_r1r2r3_fixed_budget_winner_table.csv`; `395_independent_collapse_audit_summary.json` | Strong for this panel and budget; weak for broader generalization | “under the evaluated fixed-budget protocol, METIS won 11 of 12 instances and KaHIP won 1, indicating strong multilevel dominance in this panel” | “METIS is universally best” / “metaheuristics are theoretically inferior” / “No-Free-Lunch was disproved” | This is a diagnostic result for the evaluated implementation portfolio, panel, environment, and budget. |
| C-SELECTOR-BLOCKED-BY-DEGENERACY-001 | The current fixed-budget labels do not by themselves justify a strong selector-utility claim. | EXP-BENCH-MAIN-001 | `393_main_r1r2r3_fixed_budget_summary.json`; `395_independent_collapse_audit_summary.json` | Strong as a claim-governance restriction | “because the fixed-budget winner labels are nearly degenerate, selector utility must remain conditional on later SBS/VBS, exception, budget-aware, or expanded-panel diagnostics” | “a selector is useful because it can be trained” / “the current winner table proves ASP benefit” | Blocks strong fixed-budget selector claims until Milestones 3 and 7 provide additional evidence. |
| C-MULTILEVEL-REFERENCE-001 | Exception diagnostics use the best feasible collapsed observation among METIS and KaHIP as the multilevel reference on each `(instance, budget)` slice. | D-021; methodology canon | None | Governance / methodological definition | “the exception analysis compares non-multilevel participants against the best multilevel reference available on the same slice” | “METIS is assumed universally optimal” / “the multilevel reference is an oracle outside the portfolio” | Supports definition only; empirical exception claims require completed diagnostics. |
| C-EXCEPTION-TAXONOMY-001 | Strong, near-tie, competitive, temporal, implementation-maturity, and selector-level exceptions are predefined diagnostic categories. | D-021; methodology canon | None | Governance / methodological definition | “exceptions are classified using preregistered thresholds and diagnostic categories” | “exceptions were identified post hoc with flexible thresholds” / “any close-looking case counts as an exception” | Near-tie threshold is 1%; competitive threshold is 5%; edge cut is minimized. |
| C-STRONG-EXCEPTION-001 | A non-multilevel algorithm is a strong exception only when it beats the multilevel reference on the same slice. | Future exception diagnostics | To be generated | Prospective only | “strong exceptions were/weren’t observed under the frozen definition” | “a metaheuristic is an exception because it is conceptually different” | Requires completed fixed-budget or budget-aware diagnostics. |
| C-NEAR-TIE-COMPETITIVE-001 | A non-multilevel algorithm may be classified as a near tie or competitive case when its relative gap to the multilevel reference falls within the frozen margins. | Future exception diagnostics | To be generated | Prospective only | “near ties use a 1% relative edge-cut gap and competitive cases use a 5% gap” | “near-tie thresholds were chosen after seeing the result” | Thresholds are methodological guardrails, not statistical significance tests. |
| C-SELECTOR-UTILITY-CONDITIONAL-001 | Selector usefulness is conditional on evidence of exceptions, nontrivial SBS/VBS gap, or meaningful budget-aware diversity. | D-021; future selector diagnostics | To be generated | Governance now; empirical later | “selector claims remain conditional until exception and SBS/VBS diagnostics show nontrivial signal” | “training CART is sufficient evidence of selector usefulness” | Prevents overclaim from degenerate labels. |
| C-FIXED-BUDGET-SBS-VBS-001 | In the current fixed-budget table, the SBS is essentially tied with the VBS, so a winner-selector has negligible oracle gain. | EXP-FIXED-BUDGET-EXCEPTION-DIAGNOSTICS-001 | `424_fixed_budget_exception_diagnostics_corrected_summary.json` | Strong for the evaluated panel and budget | “at `T*=5000 ms`, METIS is the SBS and its total quality differs from the VBS by only 0.001216%” | “the selector has already demonstrated useful winner prediction” / “ASP is unnecessary in general” | Supports a negative or limited fixed-budget winner-selector interpretation, not a universal claim. |
| C-FIXED-BUDGET-COMPETITIVE-R1-001 | Although no non-multilevel algorithm wins at `T*=5000 ms`, ILS is near-tie or competitive against the multilevel reference on several R1 instances. | EXP-FIXED-BUDGET-EXCEPTION-DIAGNOSTICS-001 | `421_fixed_budget_exception_diagnostics_instance_gaps.csv`; `424_fixed_budget_exception_diagnostics_corrected_summary.json` | Strong for fixed-budget diagnostic description; limited for generalization | “the fixed-budget exception diagnostics found 5 near ties and 7 competitive cases, all in R1 and all with ILS as the best metaheuristic” | “ILS beats METIS/KaHIP” / “metaheuristics dominate R1” / “R2/R3 show the same behavior” | Competitive means within the frozen 5% relative edge-cut gap, not winner status. |
| C-FIXED-BUDGET-OVERSHOOT-COMPETITIVE-001 | The competitive fixed-budget ILS cases are not solely artifacts of post-budget overshoot. | EXP-FIXED-BUDGET-EXCEPTION-DIAGNOSTICS-001 | `423_competitive_overshoot_audit_summary.json`; `424_fixed_budget_exception_diagnostics_corrected_summary.json` | Partial to strong for the competitive-case caveat | “all 35 audited competitive-case ILS runs had checkpoints before `T*=5000 ms`, and all remained competitive when evaluated at the best checkpoint before the budget” | “overshoot is irrelevant” / “all timeout behavior is harmless” / “hard caps were exact” | Overshoot remains a caveat; this row only supports the competitive-case sensitivity claim. |
| C-FIXED-BUDGET-CART-ELIGIBILITY-001 | Fixed-budget CART should not be presented as a strong multiclass winner selector on the current table; at most it may be explored as a limited exception/competitiveness detector. | EXP-FIXED-BUDGET-EXCEPTION-DIAGNOSTICS-001 | `424_fixed_budget_exception_diagnostics_corrected_summary.json` | Strong as claim governance for the current fixed-budget table | “fixed-budget CART is limited to exploratory exception/competitiveness detection because the winner labels are nearly degenerate and SBS is essentially tied with VBS” | “the fixed-budget selector is a central positive result” / “CART validates the ASP hypothesis” | Selector training still requires a selector-ready dataset and outer holdout if pursued. |
| C-BUDGET-AWARE-PROTOCOL-001 | The budget-aware exception analysis uses a preregistered temporal grid and hard cap before any `y_i*(t)` labels are constructed. | EXP-BUDGET-AWARE-PROTOCOL-001; D-022 | None | Governance / protocol only | “the budget-aware analysis was preregistered on the grid `[100, 250, 500, 1000, 2000, 3000, 4000, 5000]` ms with hard cap `T*=5000 ms`” | “the budget-aware selector improves recommendations” / “temporal exceptions were found” / “post-budget overshoot defines temporal labels” | This row supports protocol prose only; result claims require later mapped diagnostics. |
| C-BUDGET-AWARE-YIT-001 | The preregistered budget-aware analysis produced a `y_i*(t)` table over 12 instances and 8 budgets. | EXP-BUDGET-AWARE-YIT-001 | `442_budget_aware_yit_summary.json`; `443_budget_aware_yit_validation_summary.json` | Strong for table existence and structural validity | “the budget-aware table contains 96 winner labels over the preregistered grid” | “the budget-aware selector is validated” / “all temporal claims are complete without caveats” | Supports construction and validation of the table only. |
| C-BUDGET-AWARE-AVAILABILITY-100MS-001 | At `100 ms`, non-multilevel winners appear because point-output multilevel baselines are not available on all slices. | EXP-BUDGET-AWARE-YIT-001 | `444_budget_aware_temporal_exception_synthesis_summary.json` | Strong for availability-qualified wording | “at 100 ms, GRASP and ILS win some labeled slices, but this reflects early availability because the multilevel reference is often unavailable” | “GRASP and ILS beat METIS at 100 ms” / “metaheuristics dominate early budgets” | Availability-qualified temporal claim only. |
| C-BUDGET-AWARE-NO-STRONG-EXCEPTION-001 | No strong non-multilevel exception was observed at any preregistered budget. | EXP-BUDGET-AWARE-YIT-001 | `442_budget_aware_yit_exception_table.csv`; `444_budget_aware_temporal_exception_synthesis_summary.json` | Strong for the evaluated grid and panel | “no non-multilevel algorithm beat the multilevel reference on any preregistered budget slice” | “non-multilevel methods cannot ever beat multilevel solvers” / “No-Free-Lunch is disproven” | Applies only to this panel, implementation portfolio, and temporal grid. |
| C-BUDGET-AWARE-SELECTOR-ELIGIBILITY-001 | Budget-aware CART is not eligible as a strong selector claim on the current table; at most, it may be explored as an availability/competitiveness detector. | EXP-BUDGET-AWARE-YIT-001 | `444_budget_aware_temporal_exception_synthesis_summary.json` | Strong as claim governance | “budget-aware selector analysis is limited to exploratory availability and competitiveness patterns because temporal transitions are availability-driven and no strong exception was observed” | “temporal transitions prove ASP utility” / “budget-aware CART is a central positive result” | Any future model must use instance-level splits and be framed as limited unless new evidence supersedes this result. |
